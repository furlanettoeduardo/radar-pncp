<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.furlanettoeduardo</groupId>
    <artifactId>radar-pncp</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>radar-domain</artifactId>
  <name>radar-domain</name>
  <description>Pure domain model and business rules. No framework, at compile time or runtime.</description>

  <dependencies>
    <!--
      Production scope is intentionally empty and must stay that way.
      Everything below is test scope only.
    -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>net.jqwik</groupId>
      <artifactId>jqwik</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-domain-purity</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <bannedDependencies>
                  <searchTransitive>true</searchTransitive>
                  <excludes>
                    <exclude>org.springframework*:*</exclude>
                    <exclude>jakarta.persistence:*</exclude>
                    <exclude>javax.persistence:*</exclude>
                    <exclude>org.hibernate*:*</exclude>
                    <exclude>com.fasterxml.jackson*:*</exclude>
                  </excludes>
                  <message>
                    radar-domain must stay framework free. Serialization, persistence and
                    dependency injection belong in the adapters. If a domain type needs an
                    annotation, the answer is a DTO in the adapter, not a relaxed rule.
                    See docs/adr/0002-bom-import-and-enforced-domain-purity.md.
                  </message>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
