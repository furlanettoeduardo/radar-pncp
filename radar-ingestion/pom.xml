<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.furlanettoeduardo</groupId>
    <artifactId>radar-pncp</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>radar-ingestion</artifactId>
  <name>radar-ingestion</name>
  <description>PNCP client, scheduler, SQS producer and consumer, LLM enrichment.</description>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.awspring.cloud</groupId>
        <artifactId>spring-cloud-aws-dependencies</artifactId>
        <version>${spring-cloud-aws.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>io.awspring.cloud</groupId>
      <artifactId>spring-cloud-aws-starter-sqs</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.furlanettoeduardo</groupId>
      <artifactId>radar-domain</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.furlanettoeduardo</groupId>
      <artifactId>radar-shared</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!--
      Resilience4j core only, not resilience4j-spring-boot3. The Spring integration works through
      AOP proxies, and a call made from inside a virtual thread subtask does not go through one:
      the annotations would silently do nothing. Decoration here is programmatic, and the core
      modules cost a fraction of the heap.
    -->
    <dependency>
      <groupId>io.github.resilience4j</groupId>
      <artifactId>resilience4j-retry</artifactId>
      <version>${resilience4j.version}</version>
    </dependency>
    <dependency>
      <groupId>io.github.resilience4j</groupId>
      <artifactId>resilience4j-circuitbreaker</artifactId>
      <version>${resilience4j.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <version>${wiremock.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>localstack</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <properties>
    <!--
      spring-cloud-aws 3.x, not 4.x: 4.x targets Spring Boot 4 and this project is pinned to 3.5.
      Measured cost of adding it, one process, -Xmx256m, 25s after start: +23.0 MiB RSS and
      +11.8 MB on the jar. See docs/configuration.md.
    -->
    <spring-cloud-aws.version>3.4.0</spring-cloud-aws.version>
    <resilience4j.version>2.4.0</resilience4j.version>
    <wiremock.version>3.13.2</wiremock.version>
  </properties>

  <build>
    <plugins>
      <!--
        StructuredTaskScope is a preview API in Java 21, so the flag is needed at compile time and
        at run time. Three places carry it: here, the test JVMs below, and the Dockerfile
        ENTRYPOINT. It is deliberately NOT in JAVA_TOOL_OPTIONS: docker compose sets that variable
        and replaced it wholesale, which stripped the flag and broke startup. Miss one and the
        failure is a class loading error at startup, not a build error, which is why
        PreviewFlagWiringTest asserts these three and PreviewFeatures checks at boot.
        See docs/adr/0008.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <parameters>true</parameters>
          <compilerArgs>
            <arg>--enable-preview</arg>
            <arg>-Xlint:all,-serial,-processing,-preview</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>--enable-preview</argLine>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <argLine>--enable-preview</argLine>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>repackage</id>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
          <execution>
            <id>build-info</id>
            <goals>
              <goal>build-info</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
