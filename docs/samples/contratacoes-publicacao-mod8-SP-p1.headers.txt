# contratacoes-publicacao-mod8-SP-p1
# Modality 8, a single day, fetched to compare record shape across modalities.
# Collected by hand rather than by fetch-samples.ps1, so the response headers were not
# captured. Only the query is recorded here.
fetched-at: 2026-09-22
request:    GET https://pncp.gov.br/api/consulta/v1/contratacoes/publicacao?dataInicial=20260917&dataFinal=20260917&codigoModalidadeContratacao=8&uf=SP&pagina=1&tamanhoPagina=10
status:     200

# VOLUME WARNING: this is a ONE day window (2026-09-17, a Thursday), while
# contratacoes-publicacao.json (modality 6) covers SEVEN days including a weekend.
# Their totalRegistros are not directly comparable, and treating them as if they were
# understates modalities 4 and 8 by roughly 7x.
#
# These files are in English because CLAUDE.md requires it; the neighbouring headers
# files are in Portuguese because fetch-samples.ps1 wrote them.

# MEASURED VOLUME, 2026-09-23, on modality 6's window (SP, 2026-09-15..21, tamanhoPagina=10),
# so the modalities are comparable to each other:
#   modality 6: 1,707 records / 171 pages / 24.4 pages per day  (measured)
#   modality 8: 3,438 records / 344 pages / 49.1 pages per day  (measured)
#   modality 4: the seven day query failed four times; bounded at <= 4 pages per day (estimate)
# Modality 8 is the largest of the three and is NOT configured: adding it would roughly triple
# a run. See docs/adr/0010-ingestion-runs-daily.md.
#
# Also verified on 2026-09-23: tamanhoPagina=50 is accepted, giving 69 pages instead of 344 for
# this modality and window. Not adopted: time to first byte was 3.7s at page size 50 against
# 0.35-1.04s at 10, which would leave the 10s read timeout only ~2.7x headroom.
